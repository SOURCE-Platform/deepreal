# Retained research

This directory separates historical evidence from the current design baseline.

- `component-sources-legacy.md` preserves vendor-source findings.
- `drum-internals-feasibility-legacy.md` preserves the earlier geometry study
  and clearly marks its removed FreeCAD dependencies.
- `legacy-layouts/` contains the study's generated diagrams.
- `DeepReal_System_Architecture_v0.1_historical.docx` is retained only as the
  superseded Word snapshot; v0.2 in `docs/system-architecture/` is current.
